"""The pure half: no network, no clock, no randomness.

City configuration, the feature contract, geometry repair, the height chain, solar position and
the value pass. Every module here is a function of its inputs, which is what makes a district
rebuildable and a test possible without a fixture server."""
